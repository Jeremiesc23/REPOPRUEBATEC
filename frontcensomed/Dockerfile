FROM node:20-alpine AS builder
WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .
RUN npm run build

FROM node:20-alpine
WORKDIR /app
RUN npm i -g serve
COPY --from=builder /app/build ./build

ENV PORT=8080
EXPOSE 8080
CMD ["sh", "-c", "serve -s build -l $PORT"]
